class Solution(object):
    def getConcatenation(self, nums):
        """
        :type nums: List[int]
        :rtype: List[int]
        """
        a=[]
        for i in range(len(nums)):
            a.append(nums[i])
        for i in range(len(nums)):
            a.append(nums[i])
        return a
        
        